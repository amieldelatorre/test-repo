name: New Tag Created

on:
  push:
    tags:
      - "**"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-webapi

jobs:
  # build:
  #   uses: ./.github/workflows/build.yml

  push:
    # needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker metadata amd64
        id: metadata-amd64
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=tag
            type=raw,value=latest-amd64
          flavor: |
            suffix=-amd64

      # - name: Build and push
      #   id: push-amd64
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./example
      #     platforms: linux/amd64
      #     push: true
      #     tags: ${{ steps.metadata-amd64.outputs.tags }}

      # - name: Generate Artifact Attestation
      #   uses: actions/attest-build-provenance@v2
      #   with:
      #     subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      #     subject-digest: ${{ steps.push-amd64.outputs.digest }}
      #     push-to-registry: true

      - name: Docker metadata arm64
        id: metadata-arm64
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=tag
            type=raw,value=latest-arm64
          flavor: |
            suffix=-arm64

      # - name: Build and push
      #   id: push-arm64
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./example
      #     platforms: linux/arm64
      #     push: true
      #     tags: ${{ steps.metadata-arm64.outputs.tags }}

      # - name: Generate Artifact Attestation
      #   uses: actions/attest-build-provenance@v2
      #   with:
      #     subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      #     subject-digest: ${{ steps.push-arm64.outputs.digest }}
      #     push-to-registry: true

      - name: Create and push multi-platform manifest
        run: |
          echo "ARM: ${{ steps.metadata-arm64.outputs.tags }}"
          echo "AMD: ${{ steps.metadata-amd64.outputs.tags }}"
          # docker manifest create ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}

  # release:
  # needs: [push]
  # runs-on: ubuntu-latest
  # permissions:
  #   contents: write
  # steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4

  #   - name: Update release
  #     env:
  #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       TAG: ${{ github.ref_name }}
  #     run: gh release create "$TAG" --title="$TAG" --notes-file ./CHANGELOG.md
  # run: |
  #   pwd
  #   gh release create "$TAG" \
  #     --title="$TAG"
  #     --repo="$GITHUB_REPOSITORY" \
  #     --notes-file ./CHANGELOG.md
